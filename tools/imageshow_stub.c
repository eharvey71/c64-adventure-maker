/* GUI image preview stub — not needed for script compilation */
#include <stdio.h>
void imageshow(const char*name,int argc,const char*argv[])
{
 (void)argc;(void)argv;
 printf("imageshow: GUI preview not available in this build (%s)\n",name);
}
